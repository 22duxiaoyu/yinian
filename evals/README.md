# Action AI 评测集

`insight-cases.json` 是一组可重复运行的离线护栏案例，覆盖重复信号、单条证据过度推断、驳回反馈、行动结果、提示注入和跨来源引用。

运行 `npm run eval` 会检查候选输出是否只引用真实来源、是否遵守可信度上限、是否重复被用户驳回的判断。这里的 `candidate` 是当前基线，不代表线上模型永远正确；每次修改提示词或模型后，应把真实输出写入同一结构重新跑评测，并由人工复核语义质量。
